import React from "react"

export default function MenuBar(props) {
    return (
        <div className="menu-bar">
            //button for the hiscores 
            //a clock showing the time that has passed since I started the game. 
            //A help button, which shows how towers works (paused the timer?)
        </div>
    )
}

// -hiscores--- | timer | ---help- // 